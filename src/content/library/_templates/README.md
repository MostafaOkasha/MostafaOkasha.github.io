# Adding to the library

Copy any template below into `src/content/library/`, rename it, fill it in, push.
Files in this `_templates/` folder are ignored by the build (the loader only picks up
`*.md` directly under `library/` — and anything with `draft: true` is skipped too).

One template per shelf lives here.
